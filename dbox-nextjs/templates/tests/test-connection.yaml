apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbox-nextjs.fullname" . }}-test-connection"
  labels:
    {{- include "dbox-nextjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbox-nextjs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
