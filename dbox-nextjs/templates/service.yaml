apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbox-nextjs.fullname" . }}
  labels:
    {{- include "dbox-nextjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbox-nextjs.selectorLabels" . | nindent 4 }}
